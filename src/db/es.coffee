> @w5/pair/group

export default MAP = new Map

SYNC_FAV = 1
MAP.set(
  SYNC_FAV
  (R, W, li)=>
    last_id = li.pop()
    console.log last_id
    for t from group 4,li
      console.log t
    return
)
