> @w5/pair/group

export default MAP = new Map

SYNC_FAV = 1
MAP.set(
  SYNC_FAV
  (user_id, R, W, li)=>
    last_id = li.shift()
    console.log user_id, last_id
    for i from group 4,li
      console.log i
    return
)
